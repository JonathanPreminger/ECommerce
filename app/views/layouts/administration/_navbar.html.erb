<nav class="navbar navbar-expand navbar-light bg-white">
  <a class="sidebar-toggle d-flex mr-2">
    <i class="hamburger align-self-center"></i>
  </a>


  <div class="navbar-collapse collapse">
    <ul class="navbar-nav ml-auto">
      <li class="nav-item dropdown">
        <a class="nav-icon dropdown-toggle" href="#" id="messagesDropdown" data-toggle="dropdown">
          <div class="position-relative">
            <i class="align-middle" data-feather="message-circle"></i>
            <span class="indicator"><%= orders_to_be_treated.size %></span>
          </div>
        </a>
        <div class="dropdown-menu dropdown-menu-lg dropdown-menu-right py-0" aria-labelledby="messagesDropdown">
          <div class="dropdown-menu-header">
            <div class="position-relative">
              <%= orders_to_be_treated.size %> new orders to treat
            </div>
          </div>
          <div class="list-group">
            <% last_three.each do |element| %>
              <%= link_to administration_order_path(element.id), class:"list-group-item" do %>
              <div class="row no-gutters align-items-center">
                <div class="col-2">
                  <i class="align-middle" data-feather="shopping-bag"></i>
                </div>
                <div class="col-10 pl-2">
                  <div class="text-dark"><%= element.delivery_address.country %>, <%= element.delivery_address.town %> (<%= element.delivery_address.state %>)</div>
                  <div class="text-muted small mt-1">$ <%= element.total %></div>
                  <div class="text-muted small mt-1"><%= element.created_at.to_date %></div>
                </div>
              </div>
            <% end %>
            <% end %>
          <div class="dropdown-menu-footer">
            <%= link_to 'Show all orders', administration_orders_path, class:"text-muted" %> 
          </div>
        </div>
      </li>
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="userDropdown" data-toggle="dropdown">
          <span class="d-inline-block d-md-none">
            <i class="align-middle" data-feather="settings"></i>
          </span>
          <span class="d-none d-sm-inline-block">
            <span class="text-dark">My account</span>
          </span>
        </a>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="userDropdown">
          <%= link_to 'Dashboard', administration_index_path, class:"dropdown-item" %> 
          <div class="dropdown-divider"></div>
          <%= link_to 'Settings', edit_admin_registration_path, class:"dropdown-item" %> 
          <a class="dropdown-item" href="#">Help</a>
          <%= link_to 'Sign out', destroy_admin_session_path, method: :delete, class:"dropdown-item" %> 
        </div>
      </li>
    </ul>
  </div>
</nav>